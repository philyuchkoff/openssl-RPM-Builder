image: centos:centos7

before_script:
  - echo "Before script section"
  - echo "For example you might run an update here or install a build dependency"
  - echo "Or perhaps you might print out some debugging details"

after_script:
  - echo "After script section"
  - echo "For example you might do some cleanup here"

build1:
  stage: build
  script:
    - chmod +x ./install-openssl_1.1.1i.sh
    - ./install-openssl_1.1.1i.sh
    - mv /root/rpmbuild/RPMS/x86_64/openssl-1.1.1i-1.el7.x86_64.rpm /builds/philyuchkoff/openssl-RPM-Builder/openssl-1.1.1i-1.el7.x86_64.rpm
    - echo "Do your build here"
  artifacts:
    paths:
    - openssl-1.1.1i-1.el7.x86_64.rpm
    expire_in: 1 day
